import apiDecorator from "./decorator";

export default [
    apiDecorator("banner.add", async (request, response, next) => {

    }),
    apiDecorator("banner.delete", async (request, response) => {
        
    })
]